# --- web / api -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- config / validation ---------------------------------------------------
pydantic==2.10.5
pydantic-settings==2.7.1

# --- database --------------------------------------------------------------
SQLAlchemy==2.0.37
alembic==1.14.1
psycopg[binary,pool]==3.2.3
asyncpg==0.30.0
# pgvector-python: Vector/HalfVector для SQLAlchemy 2.x
pgvector==0.3.6

# --- queue -----------------------------------------------------------------
celery==5.4.0
redis==5.2.1
# Расписание beat в Redis, а не в файле: файловая система Railway эфемерна
celery-redbeat==2.2.0

# --- http ------------------------------------------------------------------
httpx==0.28.1

# --- collectors ------------------------------------------------------------
# Telethon 1.x — user session (не Bot API). v2 на PyPI ещё нет.
Telethon==1.38.1

# --- logging ---------------------------------------------------------------
structlog==24.4.0

# --- utils -----------------------------------------------------------------
tenacity==9.0.0
python-dateutil==2.9.0.post0

# --- локальные эмбеддинги (опционально) ------------------------------------
# Ставится только при EMBEDDING_PROVIDER=local: тянет torch (~2 GB).
# Раскомментируй, если не хочешь отдавать тексты в OpenAI.
# sentence-transformers==3.3.1
